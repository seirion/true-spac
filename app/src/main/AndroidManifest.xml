<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrueSpac">

        <!-- WorkManager 자동 초기화 비활성화 (Configuration.Provider 사용) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrueSpac.Splash"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="truespac"
                    android:host="app" />
            </intent-filter>
        </activity>

        <!-- 주식 시세 업데이트 알람 Receivers -->
        <receiver
            android:name=".worker.StockPriceAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".worker.TradingStartReceiver"
            android:exported="false" />
        <receiver
            android:name=".worker.TradingEndReceiver"
            android:exported="false" />

        <!-- DART 공시 업데이트 알람 Receivers -->
        <receiver
            android:name=".worker.DartAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".worker.DartUpdateStartReceiver"
            android:exported="false" />
        <receiver
            android:name=".worker.DartUpdateEndReceiver"
            android:exported="false" />

        <!-- Firebase Cloud Messaging: 푸시 알림 수신 및 토큰 갱신 처리 -->
        <service
            android:name=".service.TrueFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_id"/>

        <!-- Resolve conflict between Firebase Analytics and AdMob -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />
    </application>

</manifest>
